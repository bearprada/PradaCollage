<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab.prada.collage" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="lab.prada.collage.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="lab.prada.collage.TextEditorActivity"
            android:label="Edit Text"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="lab.prada.collage.SplashActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="lab.prada.collage.MultipleImagePickerActivity"
            android:label="@string/title_activity_multiple_image_picker"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>